<!DOCTYPE html>
<html>
  <head>
    <title>Handshake</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="title" content="Handshake">
    <meta name="description" content="Handshake is an app aimed at travelers who seek language support by providing them contextualized translations according to their needs.">
    <meta name="keywords" content="translation, translate, translating, travel, traveling, handshake, communicating, communication, communicate, interpret, render, convert, trip, contact, travelers, talk, speech, plane, car, bus, world, phone, app, context, locals, euro-trip, trip around the world">
    <meta name="robots" content="index, nofollow">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="language" content="English">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nerko+One&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Rubik&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@500&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Catamaran:wght@500&display=swap" rel="stylesheet">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
    <%= render 'shared/android_pwa_links' %>

  </head>

  <body>
    <%= render "shared/flashes" %>
  <div class="wrapper">
    <div id="content-goes-here"> <!-- the id here is just to remind you -->
      <%= yield %>
    </div>
    <div id="footer"> <!-- the id here is just to remind you -->
      <%= render "shared/navbar" %>
    </div>
  </div>
  </body>
</html>
